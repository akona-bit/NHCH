import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const { data, error } = await supabase.from('cau_hoi').select('ma_cau_hoi, noi_dung, tinh_trang, nguoi_tao, users(ho_ten)').limit(1); console.log('Error:', error); console.log('Data:', data); } run();
